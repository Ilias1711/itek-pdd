[project]
name = "itek-exam-backend"
version = "0.1.0"
description = "Backend for the ITEK Exam examination system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
]
